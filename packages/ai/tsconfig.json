{
  "extends": "@repo/typescript-config/internal-package.json",
  "compilerOptions": {
    "declaration": true,
    "declarationMap": true,
    "skipLibCheck": true,
    "rootDir": "src",   // Explicitly set the root directory
    "outDir": "dist"    // Explicitly set the output directory (matches tsup default)
  },
  "include": ["src/**/*"], // Keep the explicit include pattern
  "exclude": ["node_modules", "dist"] // Also exclude dist to prevent issues
}