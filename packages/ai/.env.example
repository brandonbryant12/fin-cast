# Example environment variables for @repo/ai
# IMPORTANT: These variables should NOT be set in this package.
# They MUST be defined in the `.env` file of the consuming application (e.g., apps/server/.env)
# with an appropriate prefix (e.g., SERVER_AI_OPENAI_API_KEY).

# OpenAI API Key (required for LLM and TTS features)
OPENAI_API_KEY=

# TTS Specific Configuration (Optional)
# OPENAI_TTS_MODEL=tts-1 # or tts-1-hd
# OPENAI_TTS_VOICE=alloy # Example voice
# OPENAI_TTS_FORMAT=mp3 # Example format

# --- OpenAI Configuration ---
# Required: Your OpenAI API key.
# Example in consuming app: SERVER_AI_OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AI_OPENAI_API_KEY=

# Optional: The base URL for the OpenAI API (useful for proxies or Azure OpenAI).
# Defaults to OpenAI's standard endpoint if not provided.
# Example in consuming app: SERVER_AI_OPENAI_BASE_URL=https://yourapi.com/v1
AI_OPENAI_BASE_URL=

# --- Anthropic Configuration (Example) ---
# Required if using Anthropic: Your Anthropic API key.
# Example in consuming app: SERVER_AI_ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# AI_ANTHROPIC_API_KEY=

# --- Add other provider configurations as needed --- 