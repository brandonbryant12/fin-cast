{
  "extends": "@repo/typescript-config/internal-package.json",
  "compilerOptions": {
    "rootDir": "src"
    // noEmit is false due to extends internal-package.json
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}